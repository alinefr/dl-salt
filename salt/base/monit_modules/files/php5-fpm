check process php-fpm with pidfile /run/php5-fpm.pid
  group www-data
  start program = "service php5-fpm start"
  stop program  = "service php5-fpm stop"
  if failed unixsocket /run/php5-fpm.sock then restart
  if 3 restarts within 5 cycles then timeout

